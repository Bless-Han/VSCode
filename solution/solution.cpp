#include <iostream>
#include <algorithm>
#include <vector>
#include <map>
using namespace std;
bool isValid(string s);
int main()
{
    freopen("input.txt", "r", stdin);
    int n;
    cin >> n;
    string s_name, s_id;
    int s_score;
    string max_name, max_id;
    int max_score;
    string min_name, min_id;
    int min_score;
    for (int i = 0; i < n; ++i)
    {
        cin >> s_name >> s_id >> s_score;
        cout << s_score << endlid;
        if (i == 0)
        {
            max_name = min_name = s_name;
            max_id = min_id = s_id;
            max_score = min_score = s_score;
        }
    }
    for (i = 0; i < count; ++i) {
        std::cout <<  << std::endl;
        std::cout <<  << std::endl;
        cout <<  << endl;
        std::cin >> ;
        cin >> ;
    }
}

